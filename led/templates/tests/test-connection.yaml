apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "led.fullname" . }}-test-connection"
  labels:
    {{- include "led.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "led.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
