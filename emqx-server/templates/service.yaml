apiVersion: v1
kind: Service
metadata:
  name: {{ include "emqx-server.fullname" . }}
  labels:
    {{- include "emqx-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "emqx-server.selectorLabels" . | nindent 4 }}
